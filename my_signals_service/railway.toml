[build]
builder = "DOCKERFILE"
dockerfilePath = "my_signals_service/Dockerfile"

[deploy]
startCommand = "./start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/api"
healthcheckTimeout = 300
