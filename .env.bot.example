# Railway project: proactive-healing (PumpingBot MT5)
# Copy values into Railway → web → Variables

SECRET_KEY=change-me-long-random
DATABASE_URL=sqlite:///./goldbot.db

# Trading — no MetaAPI
TRADING_BACKEND=agent
USE_METAAPI=0
# Same value must be in Windows VPS config.bat (default below matches code fallback)
VPS_SECRET=pumpingbot-vps-live-2026

# Optional — leave My Signals alone; bot does not need it to trade
EMBED_MY_SIGNALS=0

# Optional
ADMIN99_PASSWORD=Goku.k.g99
EMAIL_USER=
EMAIL_PASS=
ADMIN_EMAIL=pumpingbot333@gmail.com
