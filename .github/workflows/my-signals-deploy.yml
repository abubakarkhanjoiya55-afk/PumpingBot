name: Deploy My Signals (standalone)

# Disabled for now — focus is PumpingBot. Manual only if needed later.
on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "My Signals auto-deploy paused — PumpingBot only"
